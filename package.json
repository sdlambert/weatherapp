{
  "name": "weatherapp",
  "version": "0.0.1",
  "description": "A simple weather app that consumes the Weather Underground API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^2.0.0"
  }
}
